// Fired on the window when the logged-in user's avatar changes, so
// components that fetched it independently (e.g. the navbar) know to refetch.
export const AVATAR_UPDATED_EVENT = "hzla:avatar-updated";
